/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.pryaoh.boilerplate.app;

import com.pryaoh.boilerplate.list.LinkedList;

import static com.pryaoh.boilerplate.utilities.StringUtils.join;
import static com.pryaoh.boilerplate.utilities.StringUtils.split;
import static com.pryaoh.boilerplate.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
